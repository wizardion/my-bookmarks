export enum BookmarkTypes {
  FOLDER = 0,
  LINK = 1
}

export enum StatusCodes {
  ok = 0,
  success = 1,
  timeout = 2,
  redirected = 3,
  unsuccessful = 4,
  forbidden = 5,
  error = 6,
  lost = 7,
  canceled = 8,
  down = 9,
}